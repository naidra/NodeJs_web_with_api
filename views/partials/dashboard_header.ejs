<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <link rel="icon" href="images/JS.png" type="image/x-icon">
    <link href="css/bootstrap_5.3.2.css" rel="stylesheet">
    <script src="js/chart.js"></script>
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-white shadow-sm">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">
                <img src="https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=100" alt="Logo" width="40" height="40" class="rounded-circle">
                Dashboard
            </a>
            <div class="d-flex align-items-center gap-3">
                <div class="d-flex align-items-center">
                    <img src="<%- user ? user.gravatar : '' %>" class="rounded-circle me-2" width="40" height="40" alt="User">
                    <div class="d-none d-sm-block">
                        <h6 class="mb-0"><%- user ? user.name : '' %></h6>
                        <small class="text-muted"><%- user ? user.email : '' %></small>
                    </div>
                </div>
                <a href="/logout" class="btn btn-outline-danger btn-sm">Logout</a>
            </div>
        </div>
    </nav>